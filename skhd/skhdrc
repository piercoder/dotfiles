# #############################################################################
# skhdrc-macos — skhdrc for macOS
# Author: Pierpaolo Pattitoni (@piercoder)
# #############################################################################

# Open Terminal (Kitty)
alt - return : /Applications/kitty.app/Contents/MacOS/kitty --single-instance -d ~

# Open Finder
alt - f : open -a "Finder"

# Open Mail.app (Apple Mail)
alt - m : open -a "Mail"

# Open Safari
alt - b : open -a "Safari"

# Open ChatGPT
alt - g : open -a "ChatGPT"

# Open Visual Studio Code
alt - e : open -na "Visual Studio Code"

# Show desktop
alt - d : osascript -e 'tell application "System Events" to set visible of every process whose visible is true to false'

# Shutdown
alt + shift - q : osascript -e 'tell application "System Events" to shut down'

# Restart
alt + shift - r : osascript -e 'tell application "System Events" to restart'